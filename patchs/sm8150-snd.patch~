diff --git a/sound/soc/qcom/sm8150.c b/sound/soc/qcom/sm8150.c
index 3ef43aa18..99f6b6272 100644
--- a/sound/soc/qcom/sm8150.c
+++ b/sound/soc/qcom/sm8150.c
@@ -206,7 +206,7 @@ static int sm8150_mi2s_snd_hw_params(struct snd_pcm_substream *substream,
 	int ret, j;
 
 	for_each_rtd_codec_dais(rtd, j, codec_dai) {
-		ret = snd_soc_dai_set_sysclk(codec_dai, 0,
+		ret = snd_soc_dai_set_sysclk(codec_dai, Q6AFE_LPASS_CLK_ID_QUAD_MI2S_IBIT,
 					     12288000, SND_SOC_CLOCK_IN);
 		if (ret < 0)
 			dev_info(codec_dai->dev, "set_sysclk not supported (%d)\n", ret);
@@ -370,7 +370,7 @@ static int sm8150_snd_startup(struct snd_pcm_substream *substream)
 		unsigned int bclk = 12288000;
 		codec_dai_fmt |= SND_SOC_DAIFMT_NB_NF | SND_SOC_DAIFMT_I2S;
 		if (++(data->quat_mi2s_clk_count) == 1) {
-			sm8150_vote_quat_mi2s_clks(cpu_dai, bclk);
+			snd_soc_dai_set_sysclk(cpu_dai, Q6AFE_LPASS_CLK_ID_QUAD_TDM_IBIT, bclk, SND_SOC_CLOCK_OUT);
 			if (data->lpass_macro && !data->lpass_macro_enabled) {
 				int cret = clk_prepare_enable(data->lpass_macro);
 				if (!cret) {
@@ -414,7 +414,7 @@ static void sm8150_snd_shutdown(struct snd_pcm_substream *substream)
 		if (substream->stream == SNDRV_PCM_STREAM_PLAYBACK) {
 			if (data->quat_mi2s_clk_count > 0 &&
 			    --(data->quat_mi2s_clk_count) == 0) {
-				sm8150_vote_quat_mi2s_clks(cpu_dai, 0);
+				snd_soc_dai_set_sysclk(cpu_dai, Q6AFE_LPASS_CLK_ID_QUAD_MI2S_IBIT, 0, SND_SOC_CLOCK_OUT);
 			}
 			if (data->lpass_macro_count > 0 &&
 			    --(data->lpass_macro_count) == 0)
